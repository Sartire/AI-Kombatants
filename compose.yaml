services:
  python:
    build: .
    platform: linux/amd64
    ports:
      - 8889:8889
      - 6006:6006
    volumes:
      - ./code:/usr/src/code
      - ./data:/usr/src/data
    command: jupyter lab --ip 0.0.0.0 --allow-root --port 8889